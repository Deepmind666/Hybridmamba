# Code

This directory contains the mutable training codebase.

Current policy:

- all backbone and config changes go under `code/tinyvim`
- do not patch `external/` repos unless a local copy is intentionally promoted into `code/`

